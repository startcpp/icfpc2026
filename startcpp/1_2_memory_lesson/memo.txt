X ... A>0なら時計回り, A=0なら直進, A<0なら反時計回り
b ... b=A 
d ... b>0なら時計回り, b<=0なら直進
a ... b>0なら反時計回り, b<=0なら直進
m ... bを1減らす

s ... 最も近いパイプに送信
r ... 最も近いパイプから受信
R ... 読み取り順に受信

rとRの違い ... 複数のパイプから送信されていたときの優先度
               FIFOにしたいならRを使う。

---

メイン部屋
まずクエリの書き込む値を +C (C=200万) する。
読み込んだあとに -C する。

クエリの書き込む値が正の値の場合を考える。
読み込みのときは「0を書き込む命令」を部屋に送る。

読み込みのときはx=0, そうでなければxは100万～300万

---

サブ部屋

Aにxの値を読み込ませる
A=0ならBの値(+C)を送信する
A>0ならBにxを書き込む (M: x b -> x x)

---
